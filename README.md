# Level 2031

Version 2 PWA — iPhone-ready build.

Files are intended to be uploaded to the root of a GitHub Pages repository.
The service worker cache is versioned as `level2031-v2` and automatically clears older caches.
